<?php require 'includes/db.php';session_start();$err='';if($_SERVER['REQUEST_METHOD']==='POST'){$e=trim($_POST['email']??'');$p=$_POST['password']??'';$q=$pdo->prepare('SELECT * FROM admins WHERE email=?');$q->execute([$e]);$a=$q->fetch();if($a&&password_verify($p,$a['password'])){$_SESSION['admin_id']=$a['id'];header('Location: admin/index.php');exit;}else{$err='Invalid admin credentials.';}}?><!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Admin | ScholarConnect Pro</title><link rel="stylesheet" href="css/style.css"></head><body><?php include 'includes/header.php';?><section class="auth-wrap"><div class="auth-card"><span class="eyebrow">RESTRICTED AREA</span><h1>Admin</h1><p class="muted">Administrator access for managing scholarship information.</p><?php if($err):?><div class="notice"><?=htmlspecialchars($err)?></div><?php endif;?><form method="post"><label>Admin Email<input name="email" type="email" required></label><label>Password<input name="password" type="password" required></label><button class="btn btn-primary">Enter Admin</button></form></div></section><?php include 'includes/footer.php';?></body></html>
